<div id="map"
 style="width: 414px; height: 500px;"

 data-markers="<%= @markers.to_json %>"
 data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>


<div>
  <%= @parkings_spots.first['geometry']['coordinates'][0] %>
  <%= @parkings_spots.first['geometry']['coordinates'][1] %>
</div>
